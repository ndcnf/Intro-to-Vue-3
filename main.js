const app = Vue.createApp({
//   data: function() { // maniere d'ecrire avant ES6
    data() { // ES6 shorthand
        return {
            product: 'Socks',
            description: 'Some quality ones, premium ones even. No repeat.'
        }
      }
})
